# Environment Configuration for Production (Render)
NODE_ENV=production
PORT=10000

# PostgreSQL Database URL (from Render PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://user:password@localhost:5432/aimintelligence?schema=public"

# Ollama Configuration
# For production, use a cloud-hosted Ollama instance or replace with OpenAI/Anthropic API
OLLAMA_URL=http://your-ollama-instance:11434
